const frutas = ["Maça", "Banana", "Uva"];

// Opção 1: Função tradicional
frutas.forEach(function(fruta) {
    console.log(fruta);
});

// Opção 2: Arrow Function
frutas.forEach((fruta) => {
    console.log(fruta);
});